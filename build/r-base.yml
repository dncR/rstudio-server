services:
  r-base:
    image: dncr/r-base:${R_VERSION}
    build:
      context: ../
      dockerfile: dockerfiles/r-base.Dockerfile
      args:
        - R_VERSION=${R_VERSION}
        - R_HOME=${R_HOME}
        - TZ=${TZ}
        - CRAN=${CRAN}
        - LANG=${LANG}

