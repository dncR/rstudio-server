version: "3.9"

services:
  rstudio-server-4.2.0:
    image: dncr/rstudio-server:4.2.0
    build:
      context: ../
      dockerfile: dockerfiles/rstudio-server-4.2.0.Dockerfile
      
  mlseq-devel:
    image: dncr/rstudio-server:mlseq-devel-4.1.1
    build:
      context: ../
      dockerfile: dockerfiles/rstudio-mlseq.Dockerfile


