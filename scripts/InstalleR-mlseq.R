# Install R packages
install.packages(c("dplyr", "ggplot2", "caret", "pamr", "xtable", "e1071", "kernlab"))

# Install BIOCONDUCTOR packages
BiocManager::install(c("DESeq2", "edgeR", "limma", "VennDiagram", "sSeq"))
